# CI environment variables for type generation
# This file contains dummy values for CI builds to ensure proper TypeScript types are generated
# Real secrets should be managed via GitHub Secrets or Cloudflare Workers secrets

# Development settings (dummy values for CI)
API_DEV_DISABLE_RATE_LIMITS="0"
API_DEV_USE_DANGEROUS_GLOBAL_KEY="0"

# Cloudflare configuration (dummy values for CI type generation)
CLOUDFLARE_API_TOKEN="dummy-token-for-ci-types"
CLOUDFLARE_ACCOUNT_ID="def50674a738cee409235f71819973cf"

# JWT authentication (dummy value for CI)
API_JWT_SECRET="dummy-jwt-secret-for-ci-types"

# Nuxt configuration
NUXT_PUBLIC_API_BASE_URL="/api"